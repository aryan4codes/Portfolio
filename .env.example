# Email Configuration for Contact Form
# These environment variables are required for the email system to work

# SMTP Configuration (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Email Credentials
# For Gmail, use your email address and an App Password (not your regular password)
# To generate an App Password: Google Account > Security > 2-Step Verification > App passwords
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Recipient Email (where contact form submissions will be sent)
EMAIL_TO=av.rajpurkar@gmail.com

# Alternative SMTP Providers:
# 
# For Outlook/Hotmail:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
#
# For Yahoo:
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587
#
# For custom domain/hosting:
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=587 (or 465 for SSL)